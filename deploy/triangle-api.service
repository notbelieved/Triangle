[Unit]
Description=Triangle API (Node)
After=network.target postgresql.service

[Service]
Type=simple
User=root
WorkingDirectory=/opt/triangle/server
EnvironmentFile=-/opt/triangle/server/.env
ExecStart=/usr/bin/node index.js
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
