PRIVY_APP_ID=

PRIVY_APP_SECRET=

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/triangle

PORT=3001

SOLANA_RPC_URL=https://api.devnet.solana.com

TRIANGLE_ESCROW_PROGRAM_ID=

SOLANA_AUTHORITY_PRIVATE_KEY=

# Public API URL for Solana Actions metadata (blinks)
TRIANGLE_PUBLIC_API_BASE_URL=http://localhost:3001

# ─── RWA / Oracle ─────────────────────────────────────────────────────────────

# Optional: override mock RWA price (USD × 1e6). Default = 2_000_000_000 ($2000).
# MOCK_RWA_PRICE_USD_E6=3300000000

# ─── Helius ───────────────────────────────────────────────────────────────────

# Helius API key (used for devnet RPC and webhook registration)
HELIUS_API_KEY=

# Secret for verifying Helius webhook payloads (Bearer token in Authorization header)
# Must match the "authHeader" you set when registering the webhook on Helius.
HELIUS_WEBHOOK_SECRET=

# ─── Demo wallets (devnet only, NEVER commit real keys) ───────────────────────

# Base58 private keys for the demo seller and buyer wallets.
# Generate with: solana-keygen new --outfile seller.json
# Then copy the contents of seller.json here (as JSON array [uint8,...])
# DEMO_SELLER_PRIVATE_KEY=
# DEMO_BUYER_PRIVATE_KEY=

# Devnet mint address of the rwaGOLD SPL token (created by setup-devnet-demo.sh)
DEMO_RWA_MINT=

# Web (Vite): origin used in the on-chain metadata URI for token avatar/price JSON.
# Dev: use API port so wallets fetch JSON directly: VITE_PUBLIC_METADATA_BASE=http://localhost:3001
# Prod: your deployed API origin, e.g. https://api.yourdomain.com
# VITE_PUBLIC_METADATA_BASE=http://localhost:3001
